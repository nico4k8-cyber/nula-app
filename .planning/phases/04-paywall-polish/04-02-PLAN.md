---
phase: 04-paywall-polish
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/App.jsx
  - src/components/ParentView.jsx
autonomous: true
requirements: [REQ-008, REQ-016]
must_haves:
  truths:
    - "При завершении задачи foundPrinciple (trick.name из task) сохраняется в store"
    - "ParentView показывает реальные решённые задачи с названием, принципом и звёздами"
    - "ParentView доступен из SettingsMenu и из Paywall"
    - "Пустое состояние (0 задач) показывает понятное сообщение"
  artifacts:
    - path: "src/components/ParentView.jsx"
      provides: "Реальный прогресс ребёнка для родителя"
    - path: "src/App.jsx"
      provides: "Передача foundPrinciple при completeTask + роутинг ParentView"
  key_links:
    - from: "App.jsx goOutcome()"
      to: "completeTask(taskId, stars, foundPrinciple)"
      via: "task.trick.name"
    - from: "App.jsx"
      to: "ParentView"
      via: "phase === 'parent-view'"
    - from: "ParentView"
      to: "completedTasks objects"
      via: "props + TASKS lookup"
---

<objective>
Подключить передачу foundPrinciple при завершении задачи, переписать ParentView для отображения реальных данных, интегрировать ParentView в App.jsx.

Purpose: Родитель видит конкретные задачи, которые решил ребёнок, и какие ТРИЗ-принципы были использованы.
Output: Работающий ParentView с реальными данными.
</objective>

<context>
@src/App.jsx
@src/components/ParentView.jsx
@src/store/gameStore.js
@src/tasks.js (schema: id, title, icon, category, trick.name, difficulty)
@.planning/phases/04-paywall-polish/04-01-PLAN.md
</context>

<interfaces>
<!-- Из плана 01 — новая структура gameStore -->
completedTasks: Array<{ taskId: number, stars: number, foundPrinciple: string, solvedAt: string }>
completeTask(taskId, starsEarned, foundPrinciple): void
getCompletedIds(): number[]

<!-- Из tasks.js -->
task.trick.name: string  // ТРИЗ-принцип ("Посредник", "Нить Ариадны")
task.title: string       // Название задачи
task.icon: string        // Эмодзи
task.category: string    // building mapping
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: App.jsx — передать foundPrinciple + обратная совместимость + роутинг ParentView</name>
  <files>src/App.jsx</files>
  <action>
1. **Передать foundPrinciple при завершении задачи.** В App.jsx найти ВСЕ вызовы `completeTask(task.id, sessionStars)` (их минимум 2: в goOutcome и в onWantsMore). Заменить на:
   ```js
   completeTask(task.id, sessionStars, task?.trick?.name || '')
   ```
   Это берёт ТРИЗ-принцип из метаданных задачи. Да, это не "то что ребёнок нашёл сам", а заданный принцип — но это лучшее приближение без изменения engine.js (engine не возвращает отдельное поле "found principle"). Per D-04: задача содержит trick.name, и если ребёнок дошёл до конца — он по факту применил этот принцип.

2. **Заменить все `completedTasks.includes(id)` на `getCompletedIds().includes(id)`.**
   Импортировать `getCompletedIds` из деструктуризации `useGameStore()` в начале App.
   Найти ВСЕ места где используется `.includes()` на completedTasks и заменить.
   Также обновить `completedTasks.map(String)` в секции login merge — там нужно:
   ```js
   completedTasks.map(t => String(typeof t === 'object' ? t.taskId : t))
   ```

3. **Добавить роутинг ParentView.**
   - Импортировать: `import ParentView from './components/ParentView';`
   - В phase === "paywall" добавить возможность перехода: рядом с `<Paywall>` не трогать, но добавить новое условие `phase === "parent-view"`.
   - Рендер:
     ```jsx
     {phase === "parent-view" && (
       <ParentView
         completedTasks={completedTasks}
         totalStars={totalStars}
         streak={streak}
         onUnlock={() => { setPhase("paywall"); }}
         onBack={() => setPhase("city")}
       />
     )}
     ```

4. **Добавить точку входа из SettingsMenu.**
   Найти компонент SettingsMenu (передаётся в пропсы). Добавить в его пропсы `onShowParentView={() => setPhase("parent-view")}`.
   Если SettingsMenu — отдельный компонент, добавить кнопку "Для родителей" внутри.

ВАЖНО: Не трогать engine.js. Принцип берём из task.trick.name.
  </action>
  <verify>
    <automated>cd /Users/username/Documents/triz/nula && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Все вызовы completeTask передают 3й аргумент task.trick.name
    - completedTasks.includes() заменены на getCompletedIds().includes()
    - ParentView рендерится при phase === "parent-view"
    - Есть точка входа из меню настроек
  </done>
</task>

<task type="auto">
  <name>Task 2: Переписать ParentView — реальные данные вместо хардкода</name>
  <files>src/components/ParentView.jsx</files>
  <action>
1. **Обновить пропсы:**
   ```jsx
   export default function ParentView({ completedTasks, totalStars, streak, onUnlock, onBack })
   ```

2. **Импортировать TASKS для lookup:**
   ```jsx
   import { TASKS } from '../tasks';
   const taskMap = Object.fromEntries(TASKS.map(t => [t.id, t]));
   ```

3. **Маппинг category -> building name (русский):**
   ```js
   const BUILDING_NAMES = {
     'library': 'Библиотека', 'city-hall': 'Мэрия', 'nature-reserve': 'Заповедник',
     'workshop': 'Мастерская', 'farm': 'Ферма', 'bredo': 'Бредогенератор',
     'laboratory': 'Лаборатория', 'tsar': 'Царь-гора'
   };
   ```

4. **Заменить хардкоженные артефакты на реальные карточки.** Удалить массив `artifacts`. Вместо него:
   ```jsx
   const solvedTasks = completedTasks
     .map(ct => {
       const task = taskMap[ct.taskId];
       if (!task) return null;
       return { ...ct, task };
     })
     .filter(Boolean);
   ```

5. **Hero-карточка (вместо "X / 7 артефактов"):**
   ```
   Решено {solvedTasks.length} задач · ⭐ {totalStars} звёзд · 🔥 {streak} дней подряд
   ```
   Прогресс-бар: `solvedTasks.length / TASKS.length` (процент от всех задач).

6. **Секция "Что решил ребёнок"** — список карточек:
   ```jsx
   {solvedTasks.map(({ taskId, stars, foundPrinciple, task }) => (
     <div key={taskId} className="flex items-start gap-3 p-3 bg-gray-50 rounded-xl">
       <span className="text-2xl">{task.icon}</span>
       <div className="flex-1 min-w-0">
         <div className="font-semibold text-[14px] text-gray-900">{task.title}</div>
         <div className="text-[12px] text-gray-500">
           {BUILDING_NAMES[task.category] || task.category}
           {foundPrinciple && ` · Принцип: ${foundPrinciple}`}
         </div>
       </div>
       <div className="text-[14px] text-orange-500">{'⭐'.repeat(stars || 1)}</div>
     </div>
   ))}
   ```

7. **Пустое состояние** (0 задач):
   ```jsx
   {solvedTasks.length === 0 && (
     <div className="text-center py-8 text-gray-400">
       <div className="text-4xl mb-3">🌱</div>
       <p className="text-[14px]">Ребёнок ещё не решил ни одной задачи</p>
       <p className="text-[12px]">Начните играть — здесь появится прогресс</p>
     </div>
   )}
   ```

8. **Оставить без изменений:** секцию "Что это даёт?" (3 bullet points) и CTA кнопку покупки внизу. Только обновить текст CTA: вместо "$4.99" написать "199₽/год" (соответствует Paywall.jsx).

9. **Оставить секцию promo-кода** как есть — она уже работает.
  </action>
  <verify>
    <automated>cd /Users/username/Documents/triz/nula && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - ParentView показывает реальные решённые задачи (название, иконка, здание, принцип, звёзды)
    - Пустое состояние: сообщение "ещё не решил ни одной задачи"
    - Hero-карточка: количество задач + звёзды + стрик
    - CTA кнопка с ценой 199₽/год
    - Build проходит
  </done>
</task>

</tasks>

<verification>
- `npm run build` проходит
- Открыть приложение, решить 1 задачу, перейти в ParentView через меню
- Видно: название задачи, иконка, здание, ТРИЗ-принцип, звёзды
- При 0 задач — пустое состояние с сообщением
</verification>

<success_criteria>
Родитель видит реальный прогресс ребёнка: какие задачи решены, какие ТРИЗ-принципы использованы, сколько звёзд получено.
</success_criteria>

<output>
After completion, create `.planning/phases/04-paywall-polish/04-02-SUMMARY.md`
</output>
