---
phase: 04-paywall-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/gameStore.js
autonomous: true
requirements: [REQ-008]
must_haves:
  truths:
    - "completedTasks хранит per-task объект с stars, foundPrinciple, solvedAt"
    - "Старые данные (массив ID) мигрируются автоматически при загрузке"
    - "completeTask() принимает расширенные данные и сохраняет их"
    - "completedTasks.length и .includes(id) продолжают работать во всём приложении"
  artifacts:
    - path: "src/store/gameStore.js"
      provides: "Расширенный completedTasks + миграция"
      contains: "foundPrinciple"
  key_links:
    - from: "gameStore migrate v5"
      to: "completedTasks object format"
      via: "zustand persist migrate"
---

<objective>
Расширить структуру completedTasks в gameStore: вместо массива ID хранить объекты с stars, foundPrinciple, solvedAt. Обеспечить обратную совместимость — старый формат мигрируется автоматически.

Purpose: ParentView (план 02) нуждается в per-task данных для отображения родителю.
Output: Обновлённый gameStore.js с новой структурой и миграцией.
</objective>

<context>
@src/store/gameStore.js
@.planning/phases/04-paywall-polish/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Расширить completedTasks и completeTask()</name>
  <files>src/store/gameStore.js</files>
  <action>
1. Изменить формат completedTasks с `number[]` на `object[]`:
   ```js
   completedTasks: []
   // Каждый элемент: { taskId: number, stars: number, foundPrinciple: string, solvedAt: string }
   ```

2. Обновить `completeTask(taskId, starsEarned, foundPrinciple = '')`:
   - Проверка дубликатов: `state.completedTasks.some(t => t.taskId === taskId)` вместо `.includes(taskId)`
   - Добавлять объект: `{ taskId, stars: starsEarned, foundPrinciple, solvedAt: new Date().toISOString() }`

3. Обновить ВСЕ места в gameStore.js где используется completedTasks:
   - `checkUnlocks()`: `state.completedTasks.length` — работает без изменений
   - `canPlayTask()`: без изменений (использует length)
   - `resetGame()`: `completedTasks: []` — без изменений

4. Добавить хелпер для обратной совместимости (используется в App.jsx):
   ```js
   // Хелпер: получить массив taskId для совместимости с .includes(id)
   getCompletedIds: () => get().completedTasks.map(t => typeof t === 'object' ? t.taskId : t),
   ```

5. Обновить миграцию: version 4 -> 5:
   ```js
   version: 5,
   migrate: (state, version) => {
     if (version < 4) {
       state = { ...state, adaptiveData: { adaptiveAge: 10, consecutiveClean: 0, recentStruggle: 0 } };
     }
     if (version < 5) {
       // Мигрировать старый формат completedTasks: number[] -> object[]
       const oldTasks = state.completedTasks || [];
       state = {
         ...state,
         completedTasks: oldTasks.map(t =>
           typeof t === 'object' ? t : { taskId: t, stars: 0, foundPrinciple: '', solvedAt: '' }
         ),
       };
     }
     return state;
   },
   ```

ВАЖНО: Не менять файлы кроме gameStore.js. App.jsx будет обновлён в плане 02.
  </action>
  <verify>
    <automated>cd /Users/username/Documents/triz/nula && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - completedTasks хранит массив объектов {taskId, stars, foundPrinciple, solvedAt}
    - completeTask принимает 3й аргумент foundPrinciple
    - Миграция v4->v5 конвертирует старые number[] в object[]
    - getCompletedIds() возвращает плоский массив ID для совместимости
    - Build проходит без ошибок
  </done>
</task>

</tasks>

<verification>
- `npm run build` проходит без ошибок
- В gameStore.js: `version: 5`, миграция обрабатывает старый формат
- `completeTask` принимает 3 аргумента
- `getCompletedIds()` экспортирован через store
</verification>

<success_criteria>
gameStore хранит per-task данные (stars, foundPrinciple, solvedAt) и мигрирует старый формат автоматически.
</success_criteria>

<output>
After completion, create `.planning/phases/04-paywall-polish/04-01-SUMMARY.md`
</output>
